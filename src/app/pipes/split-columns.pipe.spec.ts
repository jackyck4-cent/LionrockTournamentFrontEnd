import { SplitColumnsPipe } from './split-columns.pipe';

describe('SplitColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
